"""Variable -> data-source binding.

Configured mappings beat aliases beat built-in guesses. A guess is a starting
point so a freshly-analysed template works immediately; configuration is what
makes it correctable.
"""
